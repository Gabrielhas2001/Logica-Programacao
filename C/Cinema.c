#include <stdio.h>
#include <stdlib.h>

int main()
{
    int hora_cinema = 20;
    int hora_atual = 20;

    if (hora_cinema == hora_atual)
    {
        printf("Posso entrar");
    }
    else
    {
        printf("Nao posso entrar");
    }
    return 0;
}